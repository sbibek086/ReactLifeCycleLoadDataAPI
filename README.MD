#simple app using react lifecycle to load the data from API Response

##Installing Instructions
To run this project locally, here are the steps:

Clone this repository
Run "npm install"
Run "npm start"
